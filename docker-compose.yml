version: '3'
services:
    pose-3d:
        build:
          #contect specifies where the dockerfile is
          context: .
          #specifies the docker file to be built
          dockerfile: Dockerfile
          #Can use the args to provide input arguments for dockerfile
          #args:
          #   buildno : 1  # <- this sets the build number which is an argument in the dockerfile to 1

        container_name: pose-3d
        volumes:
          - /RemoteDockerFiles/MatthewWorkstation/pose-3d:/code/
         #put volumes here -  /path/on/system:/path/in/container

